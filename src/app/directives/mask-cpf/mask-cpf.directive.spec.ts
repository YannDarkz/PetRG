import { input } from '@angular/core';
import { MaskCpfDirective } from './mask-cpf.directive';

describe('MaskCpfDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskCpfDirective();
    expect(directive).toBeTruthy();
  });
});
